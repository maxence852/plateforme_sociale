{# On utilise ici le layout général, qui se situe dans ce même CoreBundle maintenant #}
{% extends "TfeCoreBundle::layout.html.twig" %}

{% block title %}
    Page d'accueil - {{ parent() }}
{% endblock %}
{% block stylesheets %}
    {{ parent() }}

{% endblock %}
{% block header %}
    <nav id="eee" style="border:1px solid green" class="navbar navbar-default"><p></p>
        <div class="container">
            <div class="navbar-header">
                <!-- Collapsed Hamburger -->
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#app-navbar-collapse">
                    <span class="sr-only">Toggle Navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>

                <!-- Branding Image -->
                <a class="navbar-brand" href="{{ path('tfe_user_accueil') }}">Accueil</a>
            </div>

            <div class="collapse navbar-collapse" id="app-navbar-collapse">
                <!-- Left Side Of Navbar -->
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('tfe_platform_sociale_publication') }}" style="color: white">Fil d'actualité</a></li>
                </ul>
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('tfe_forum_homepage') }}">Forum</a></li>
                </ul>

                <!-- Right Side Of Navbar -->
                <ul class="nav navbar-nav navbar-right">
                    <!-- Authentication Links -->
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}

                        <li><a href="{{ path('fos_user_security_logout') }}">Deconnexion</a></li>
                    {% else %}
                        <li><a href="{{ path('fos_user_security_login') }}">Connexion</a></li>
                        <li><a href="{{ path('fos_user_registration_register') }}">inscription</a></li>
                    {% endif %}
                    <li class="dropdown">
                        {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
                            {{ app.user.id }} <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu" role="menu">
                            <li><a href="{{ path('fos_user_profile_show') }}"><i class="fa fa-btn"></i>Paramètre du compte</a></li>
                        </ul>
                        {% endif %}
                </ul>
            </div>
        </div>

    </nav>
    {% endblock %}

{% block body %}
    <p class="text-center">Bonjour chers fiévreux du monde de l’écriture. Sur ce site vous pourrez partager vos œuvres simples ou tordues, avec des styles qui passent des poèmes aux nouvelles. Grâce à ces publications les autres utilisateurs peuvent vous critiquer constructivement afin que vous vous amélioriez et à l’inverse vous pouvez lire celles des autres pour vous divertir. Du côté du forum partager est le maître mot, alors que des écrivains gardent jalousement leurs trucs et astuces, ici on s’entre-aide !
        Aussi il via l’onglet « Schémas » vous avez la possibilité d’éditer des schémas narratifs avec leurs chapitres, scènes personnages et traits. Et ce n’est pas tout, car une application Android aussi de schémas narratifs est là pour accueillir votre inspirations n’importe où et quand, et ce tout en synchronisant avec l’application web !
        A vos plumes !
        Prets ?
        Rêvez….
    </p>

{% endblock %}

{% block footer %}
   <footer class="text-center"><p class="text-center">Site réalisé par Begon Maxence et Messina Dorian</p></footer>
{% endblock %}